# Dataset Package

The archive includes the Fujian-30 data and the fifteen final R30 PeMS sub-datasets used by StateFlowDiff.

The three original datasets, PEMS03, PEMS04, and PEMS08, are intentionally not redistributed here. Their names and the final split inventory are recorded in `DATASET_MANIFEST.json`. Dataset-splitting code is also excluded.
